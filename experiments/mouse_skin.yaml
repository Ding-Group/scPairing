files:
  mod1:
    - /arc/project/st-jiaruid-1/yinian/shareseq/mouse_skin_rna.h5ad
  mod2:
    - /arc/project/st-jiaruid-1/yinian/shareseq/mouse_skin_atac.h5ad
model_params:
  emb_dim: 3
  hidden_dims:
    - 128
  decode_features: True
  decode_method: dropout
  cell_dropout: False
trainer_params:
  n_epochs: 800
  eval_every: 800
  raw_layer:
    - raw
    - binary
  transformed_layer:
  batch_size: 500
  flip_contrastive_reconstruct: 0
  reconstruct_warmup_ratio: 0
  reconstruct_cutoff_ratio: 0
  max_reconstruct_weight: 0
cell_type_col: celltype
mod1_n_vars: 21127
ckpt_dir: /scratch/st-jiaruid-1/yinian/my_jupyter/scCLIP/results/mouse-brain/