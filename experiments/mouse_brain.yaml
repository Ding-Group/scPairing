files:
  mod1:
    - /arc/project/st-jiaruid-1/yinian/shareseq/mouse_brain_rna.h5ad
  mod2:
    - /arc/project/st-jiaruid-1/yinian/shareseq/mouse_brain_atac.h5ad
model_params:
  emb_dim: 10
  hidden_dims:
    - 128
  use_decoder: True
  decode_method: dropout
  discriminative: False
  loss_method: debiased
trainer_params:
  n_epochs: 1200
  eval_every: 300
  ping_every: 100
  counts_layer:
    - raw
    - binary
  transformed_obsm:
    - X_pca
    - X_lsi
  batch_size: 4000
  weight_decay: 0.001
cell_type_col: celltype
reconstruct: True
ckpt_dir: /scratch/st-jiaruid-1/yinian/my_jupyter/scCLIP/results/mouse-brain/